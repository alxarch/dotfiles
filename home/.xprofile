#!/bin/sh
xrdb -merge .Xresources
setxkbmap -model pc105 -layout us,gr -variant ,simple -option grp:alt_space_toggle
xmodmap "$HOME/.xmodmap"
pcmanfm --daemon-mode &
[ -f "$HOME/.fehbg" ] && source "$HOME/.fehbg"
# exec i3

